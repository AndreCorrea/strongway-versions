# Para iniciar o projeto facilmente, basta abrir o terminal (tenha um servidor PHP instalado), e rode o comando:

php -S localhost:{porta que você deseja rodar). Por exemplo:

<code>
  php -S localhost:8080
</code>
